// next.config.mjs
// Basic Next.js config - add image domains, redirects, etc. if needed
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // experimental: {
  //   serverActions: true, // Enabled by default in latest Next.js 14
  // },
};

export default nextConfig; 